var renderClass = "jp.ngt.rtm.render.OrnamentPartsRenderer";

importPackage(Packages.dev.siro256.rtmpack.siromodels.router);

function init(modelSet, modelObject) {
    RendererRouter.init(renderer, modelObject);
}
